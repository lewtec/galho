package scaffold

import "io/fs"

func InstallFS(destination string, data fs.FS) error {
	// implement copy data from fs.FS to destination
	// if extension ends with tmpl, copy without the tmpl extension
	return nil
}
